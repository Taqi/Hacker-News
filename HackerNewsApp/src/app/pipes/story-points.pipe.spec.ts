import { StoryPointsPipe } from './story-points.pipe';

describe('StoryPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new StoryPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
